services:
  bot:
    build:
      context: .
      dockerfile: Containerfile
    restart: always
    volumes:
      - './data:/app/data:rw,Z'
    env_file: .env
    tty: true
    environment:
      RUST_LOG: debug

